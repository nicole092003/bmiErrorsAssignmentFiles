document.getElementById("CalcBMI").addEventListener('click',doCalcBMI);

function doCalcBMI(){
    var theWeight = document.getElementById("myPounds").value;
    var theHeight = document.getElementById("myHeight").value;
    var myBMI = (theWeight / (theWeight * Math.pow(2)(theHeight*703)
    (myBMI)  (myBMI.toFixed(2))
    (document.getElementById("bmiResult").INNERhtml = myOutput(myBMI)
}
function myOutput(getBMI){
    let theString = "Your body mass index (BMI) is: "  
    return theString;
}
